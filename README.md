# Adib Lang Playground

Run [Adib Lang](https://github.com/adibfahimi/adib-lang) with WebAssembly in the browser.

## Usage

```bash
wasm-pack build --target web --out-dir docs
```

```
npx tailwindcss -i input.css -o docs/output.css --minify
```
